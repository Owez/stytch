[package]
name = "stytch"
description = "Unofficial glue wrapper to use the basic email flow of Stytch inside of Rust"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
reqwest = { version = "0.11", features = ["json"] }
